# Operador de Modulo es // y % devuelve el residuo de una división
edad = input("escribe tu edad: ") # todo lo que el usuario inserta es un string y se debe convertir a tipo de dato que deseamos ejemplo:
nueva_edad = int(edad) + 5
print(nueva_edad)